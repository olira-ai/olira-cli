# Extends pyproject.toml [tool.ruff]; use for pre-commit / editor
line-length = 120
target-version = "py310"

[lint]
select = ["E", "W", "F", "I", "B", "C4", "UP"]
ignore = ["E501", "B008", "C901"]

[lint.per-file-ignores]
"__init__.py" = ["F401"]

[lint.isort]
known-first-party = ["olira_cli"]
